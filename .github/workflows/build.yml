- name: Extract ZIP
      run: unzip MAWI_AI_Complete.zip -d extracted && cp -r extracted/MAWI_AI/* .

    - name: Add API Key
      run: |
        sed -i 's/YOUR_GEMINI_API_KEY_HERE/${{ secrets.GEMINI_API_KEY }}/g' app/src/main/java/com/mawi/ai/VoiceCommandService.kt
