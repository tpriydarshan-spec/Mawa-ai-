name: Build MAWI AI APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Extract and Setup Project
      run: |
        unzip -o MAWI_AI_Complete.zip
        ls -la
        ls -la MAWI_AI/

    - name: Add API Key
      run: |
        sed -i 's/YOUR_GEMINI_API_KEY_HERE/${{ secrets.GEMINI_API_KEY }}/g' MAWI_AI/app/src/main/java/com/mawi/ai/VoiceCommandService.kt

    - name: Make Gradle Executable
      run: chmod +x MAWI_AI/gradlew

    - name: Build Debug APK
      run: cd MAWI_AI && ./gradlew assembleDebug

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: MAWI-AI-APK
        path: MAWI_AI/app/build/outputs/apk/debug/app-debug.apk
